# 🚀 Инструкция по деплою на Render.com

## Шаг 1: Подготовка файлов

Убедитесь, что у вас есть следующие файлы:
- ✅ `simple_web_app.py` - веб-приложение
- ✅ `advanced_neural_chemistry.py` - ИИ движок
- ✅ `requirements_web.txt` - зависимости для веб-приложения
- ✅ `render.yaml` или `Procfile` - конфигурация для Render

## Шаг 2: Создание аккаунта на Render.com

1. Перейдите на [render.com](https://render.com)
2. Зарегистрируйтесь через GitHub (рекомендуется) или email
3. Подтвердите email, если требуется

## Шаг 3: Создание репозитория на GitHub

1. Создайте новый репозиторий на [github.com](https://github.com)
2. Название: `chemistry-ai-web-app` (или любое другое)
3. Загрузите файлы в репозиторий:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ВАШ_USERNAME/chemistry-ai-web-app.git
   git push -u origin main
   ```

## Шаг 4: Деплой на Render.com

### Вариант A: Через render.yaml (автоматический)

1. В Render.com нажмите **"New +"** → **"Blueprint"**
2. Подключите ваш GitHub репозиторий
3. Render автоматически обнаружит `render.yaml` и настроит сервис
4. Нажмите **"Apply"**

### Вариант B: Через веб-интерфейс (ручной)

1. В Render.com нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий
3. Настройки:
   - **Name**: `chemistry-ai-web-app`
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements_web.txt`
   - **Start Command**: `gunicorn --bind 0.0.0.0:$PORT simple_web_app:app`
4. Нажмите **"Create Web Service"**

## Шаг 5: Ожидание деплоя

- Render автоматически соберет и запустит приложение
- Первый деплой может занять 5-10 минут
- Вы увидите URL типа: `https://chemistry-ai-web-app.onrender.com`

## Шаг 6: Обновление URL в боте

После получения URL от Render:

1. Откройте `telegram_chemistry_bot.py`
2. Найдите строку с URL (около строки 1108):
   ```python
   web_app={"url": "https://chemistry-ai-solver.onrender.com"}
   ```
3. Замените на ваш новый URL:
   ```python
   web_app={"url": "https://ВАШ-APP-NAME.onrender.com"}
   ```

## Шаг 7: Настройка Telegram Web App

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите **"Bot Settings"** → **"Menu Button"**
5. Нажмите **"Configure Menu Button"**
6. Выберите **"Web App"**
7. Введите название: `Chemistry AI`
8. Введите URL: `https://ВАШ-APP-NAME.onrender.com`
9. Готово! Теперь кнопка меню будет открывать ваше приложение

## Проверка работы

1. Откройте вашего бота в Telegram
2. Отправьте `/start`
3. Нажмите кнопку **"🌐 Открыть Веб-Приложение"**
4. Приложение должно открыться внутри Telegram!

## Важные замечания

⚠️ **Бесплатный план Render:**
- Приложение "засыпает" после 15 минут бездействия
- Первый запрос после пробуждения может занять 30-60 секунд
- Это нормально для бесплатного хостинга

✅ **Рекомендации:**
- Используйте бесплатный план для тестирования
- Для продакшена рассмотрите платные планы или другие хостинги

## Устранение проблем

**Проблема**: Приложение не запускается
- Проверьте логи в Render Dashboard
- Убедитесь, что все файлы загружены в репозиторий
- Проверьте, что `requirements_web.txt` содержит все зависимости

**Проблема**: Ошибка импорта
- Убедитесь, что `advanced_neural_chemistry.py` находится в корне проекта
- Проверьте, что все зависимости установлены

**Проблема**: Приложение не открывается в Telegram
- Проверьте, что URL правильный и начинается с `https://`
- Убедитесь, что приложение доступно (откройте URL в браузере)
- Проверьте настройки в BotFather


